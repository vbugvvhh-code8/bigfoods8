export default function OrdersChart({ data }: any) {
  // Placeholder: charting can be added later — for now render a simple canvas wrapper
  return (
    <div style={{height: 160}} className="rounded-md bg-[rgba(0,0,0,0.03)] flex items-center justify-center">Orders chart</div>
  );
}
