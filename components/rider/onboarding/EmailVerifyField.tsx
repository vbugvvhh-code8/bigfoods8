// Same `send-email-otp` / `verify-email-otp` mechanism as the restaurant
// portal, so this re-exports that component rather than duplicating it —
// per the handoff note that this "can likely be shared."
export { default } from '@/components/restaurant/onboarding/EmailVerifyField';
